import React from "react";
import { Link } from "react-router-dom";

import { useStoreContext } from '../../utils/GlobalState';
import { } from '../../utils/actions';

function NewsLayout(news) {
    const {
        postText,
        username,
        feedName,
        replies,
        createdAt,
    }
}